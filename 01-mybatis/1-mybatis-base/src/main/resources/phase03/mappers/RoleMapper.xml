<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeffosmond.phase03.mapper.RoleMapper">

    <select id="findRoleById"
            parameterType="long"
            resultType="com.jeffosmond.phase03.entity.Role">
		SELECT * FROM role WHERE role_id = #{id}
	</select>

</mapper>